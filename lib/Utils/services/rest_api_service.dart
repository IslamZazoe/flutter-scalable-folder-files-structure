/**
 * We do call the rest API to get
 * store data on a remote database
 * write the rest API
 * We can make use of http package to make the rest API call in the flutter
 * rest call is a success or need to return custom error exception on the basis of 4xx, 5xx status code.
 */
